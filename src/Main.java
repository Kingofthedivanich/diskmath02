import tasks.InsertionSort;
import tasks.SearchComparison;

public class Main {
    public static void main(String[] args) {
        SearchComparison.search(args);
        InsertionSort.search(args);
    }
}